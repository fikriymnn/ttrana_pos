import 'package:flutter/material.dart';

class ProdukKasir extends StatelessWidget {
  const ProdukKasir({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("produk"),
      ),
    );
  }
}
