import 'package:flutter/material.dart';

class TambahStaff extends StatelessWidget {
  const TambahStaff({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Tambah staff"),
    );
  }
}