import 'package:flutter/material.dart';

class IkanManagement extends StatelessWidget {
  const IkanManagement({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}