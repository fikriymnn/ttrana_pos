import 'package:flutter/material.dart';

class RatingAplikasi extends StatelessWidget {
  const RatingAplikasi({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Rating Aplikasi"),
    );
  }
}
