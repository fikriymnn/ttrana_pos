import 'package:flutter/material.dart';

class PrinterStruck extends StatelessWidget {
  const PrinterStruck({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Printer & struck"),
    );
  }
}